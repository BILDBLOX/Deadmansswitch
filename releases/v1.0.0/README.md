# v1.0.0 prebuilt Windows binaries — superseded, broken

**Do not use these.** The Settings panel has a bug where, once opened, it
can never be closed — Save, ✕, the backdrop, and Esc all silently do
nothing, blocking the whole window.

Use [v1.0.1](../v1.0.1/) instead, which fixes this.
